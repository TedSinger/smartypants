<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parade Demo</title>
</head>
<body>
    <h1>Welcome to the Parade Demo!</h1>
    <p>This is a demo Jinja template for the Parade module.</p>
    <p>Current request path: {{ request.path }}</p>
    <p>Query parameters:</p>
    <ul>
    {% for key, value in request.GET.items() %}
        <li>{{ key }}: {{ value }}</li>
    {% endfor %}
    </ul>
</body>
</html>
